# requirements.txt
PyPDF2
tqdm
mypy

# USAGE INSTRUCTIONS
# 1. Create a virtual environment (recommended):
#    python -m venv .venv
# 2. Activate the environment:
#    On Windows: .venv\Scripts\activate
#    On Mac/Linux: source .venv/bin/activate
# 3. Install dependencies:
#    pip install -r requirements.txt
# 4. Run the script:
#    python pdfTextFinder.py
#
# The script will process all PDFs in the 'originalPDFs' folder and output results to 'pdf_outputs'.
